# 小雨手机二改版：工作范围

本目录是用户唯一允许修改的“小雨手机”二改源码：

`C:\Users\Yu\Documents\ChatGPT\nuomi\SullyOS888-workspace\SullyOS888-master`

- 必须在 `customization` 分支工作。
- 虽然上游项目及部分原始文案名为“手抓糯米机 / SullyOS”，本分支是用户自己的“小雨手机”二改版；不要把任务改到上游原版、`SullyOS-github`、任何 `SullyOS888-update-*`、`SullyOS888-pages-*` 或 `_orig_tmp_*` 目录。
- 用户用于上传的唯一成品目录是 `C:\Users\Yu\Documents\GitHub\SullyOS888`。源码修改、构建、同步和版本号均以本目录为准。
- 开始编辑前先阅读 `PROJECT_MEMORY.md`，其中记录了长期需求、发布流程和已实现功能。
- 发现本工作区有其他未提交的改动时，视为其他窗口或用户的工作：不要覆盖、还原、暂存或提交它们；仅修改当前任务所需文件。
